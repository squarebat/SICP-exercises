#lang sicp
(define (odd? x) (= (remainder 2) 1))
(define (even? x) (not (odd? x)))
(define (same-parity . w)
  (define (parity? x))
   (if (null? items)
      nil
      (append (reverse (cdr items)) (list (car items)))))