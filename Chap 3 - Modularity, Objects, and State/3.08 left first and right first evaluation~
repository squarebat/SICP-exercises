#lang scheme
(define init 10)
(define var init)
(define (f arg)
  (cond ((not (= var 0)) (set! var arg)))
  var)

;simulate both left first and right first evaluation.
(+ (f 0) (f 1))
(set! var init)
(+ (f 1) (f 0))

;we see that evaluation occurs left to right by interpreter.
